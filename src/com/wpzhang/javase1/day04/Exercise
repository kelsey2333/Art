2、当输入的row col分别为3，5时，输出结果为（3,5），因为在Cell2类中重写了toString方法
   输出结果为A
3、equals是比较两个东西是否等同，这个适用于任何类别的对象
  contains是是否包含的意思，左边的对象是一个容器了
  String也有一个contains，那是因为String是一个字符的容器
  equals是需要两个对象完全相同才会返回true，而contains是要循环遍历容器里的所有内容后判断是否包含对象
4、D
day04
1、B
2、A正确：-迭代器iterator用于遍历集合元素。获取迭代器可以使用
           Collection定义的方法：Iterator iterator();
          -迭代器是一个接口。集合在重写Collection的iterator()方法时利用内部类提供的迭代器的实现
   B错误：-迭代器的hasNext用于判断集合是否还有元素进行判断；next方法则是用于返回迭代的下一个元素
   C正确
   D错误：新循环在遍历集合时和Iterator是异曲同工；并非完全不同
3、底层实现不同：ArrayList查找速度快，增删速度快：因为直接通过索引下表进行查找，而进行增删时要进行扩容缩容等操作；
                 而LinkedList在查找时必须知道首地址，然后才能一个一个向后查找，但其进行插入删除时只要改变其中数据元素的地址即可
                 所以LinkedList查找速度慢，增删速度快
4、D
5、C


