1、
垃圾回收机制：
a、垃圾回收器是JVM自带的一个线程（自动运行着的程序），用于回收没有任何
引用指向的对象。
b、Java程序员不用担心内存管理，因为垃圾回收器会自动进行回收管理。
2、
Java会产生内存泄漏问题。不再使用的内存没有被及时的回收，严重的内存泄漏
会因为过多的内存占用而导致程序的奔溃；而GC线程判断对象是否可以回收的依据
是该对象是否有引用指引，所以当确定该对象不再使用时，应该将其引用设置为null。
3、
JVM分成三个部分：堆管理对象，栈用于存放方法中的局部变量，方法区用于存放类的信息
Foo foo = new Foo();
a、在栈中给局部变量foo开辟空间 b、执行new，在堆中开辟空间，保存对象的成员变量，赋默认值
c、new执行结束，执行方法体，通常是给成员变量进行初始化 d、将对象在内存中的首地址返回
保存到foo中
foo.f();
创建的对象是FOO，就调用FOO中的方法
4、一个子类只能继承一个父类
5、5.6--测试对象--红色